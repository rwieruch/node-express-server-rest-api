pipeline {
    agent any

      tools{
        nodejs 'node18'
    }

    stages {
        stage('Checkout') {
            steps {
                // Check out the code from Git repository
                git 'https://github.com/vamanrathod/node-express-server-rest-api'
            }
        }

        stage('Dependencies') {
            steps {
                // Build your application artifact
                sh 'npm install'
            }
        }

        stage("Docker Build & Push"){
            steps{
                script{
                   withDockerRegistry(credentialsId: 'docker', toolName: 'docker'){   
                       sh "docker build -t zomato ."
                       sh "docker tag zomato vamanrathod199/zomato:latest "
                       sh "docker push vamanrathod199/zomato:latest "
                    }
                }
            }
        }

    }
}    
